body { margin: 0; padding: 0; width: 100%; height: 100%; }

.container { width: 100%; min-height: 100%; height: auto !important; height: 100%; margin: 0; position: relative; background: #AAA; }

.group .title { background: #999; height: 50px; line-height: 50px; }

.group .content { overflow: hidden; }
.group .content.on { visibility: visible;}
.group .content.off { height: 0; visibility: hidden; }

.device { width: 33.3%; background: #DDD; float: left; }
.device .icon { width: 100px; height: 100px; margin: 10px auto 0 auto; }
.device .icon.on { background: #080; }
.device .icon.on { background: #AAA; }
.device .description { text-align: center; margin: 10px 0; }
